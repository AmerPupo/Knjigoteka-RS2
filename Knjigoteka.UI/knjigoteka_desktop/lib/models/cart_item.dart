class CartItem {
  int bookId;
  String title;
  int quantity;
  double unitPrice;
  CartItem({
    required this.bookId,
    required this.title,
    required this.quantity,
    required this.unitPrice,
  });
}
